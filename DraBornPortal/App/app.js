const canvas=document.getElementById('stars'),ctx=canvas.getContext('2d');let stars=[];function resize(){const d=Math.min(devicePixelRatio||1,2);canvas.width=innerWidth*d;canvas.height=innerHeight*d;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px';ctx.setTransform(d,0,0,d,0,0);stars=Array.from({length:Math.min(120,Math.floor(innerWidth/8))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.2+.2,v:Math.random()*.18+.04,a:Math.random()*.65+.15}))}function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);for(const s of stars){s.y-=s.v;if(s.y<0){s.y=innerHeight;s.x=Math.random()*innerWidth}ctx.globalAlpha=s.a;ctx.fillStyle='#9adfff';ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fill()}requestAnimationFrame(draw)}addEventListener('resize',resize,{passive:true});resize();draw();document.querySelectorAll('a[href*=".apk"]').forEach(a=>a.addEventListener('click',()=>{a.classList.add('downloading');setTimeout(()=>a.classList.remove('downloading'),1600)}));