# DKD AnyelaBorn Mobile Mockup v1.5

- Mobil öncelikli premium mockup tasarımı gerçek siteye geçirildi.
- Kullanıcı tarafından sağlanan Anyela görselleri `assets/images` altında kullanıldı.
- `/AnyelaBorn/`, `/packages/`, `/chat/`, `/voice/`, `/custom/`, `/ads/`, `/payment/`, `/faq/` route dosyaları hazırdır.
- Termux tarafında Vite build çalıştırmaz; statik deploy dosyalarını doğrudan push eder.
