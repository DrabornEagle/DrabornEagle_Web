# DKD Anyela Born Miami Login v3

Route:

- `/AnyelaBorn/Login/`

Changes:

- Anyela character image was replaced with the user-provided yacht portrait.
- The character background was removed into a transparent PNG asset.
- Login page was moved into the `/AnyelaBorn/Login/` route.
- `/AnyelaBorn/` redirects to `/AnyelaBorn/Login/`.
- Social login buttons were rebuilt with modern inline SVG brand icons.
- The mobile shell and login card were widened for a fuller phone-screen layout.

Notes:

- This is a visual/static login screen.
- Backend authentication is not connected yet.
